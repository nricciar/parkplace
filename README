Required:
  gem install rfuzz camping mongrel exifr --include-dependancies
  gem install sendfile

  rfuzz is used by parkplace to get updates from the master server
  exifr is used to extract metadata for jpeg images

Run:
  bin/parkplace

More Options:

  bin/parkplace --help

Replicate Your Parkplace Server:

  bin/parkplace -s master.server
